import 'package:flutter/material.dart';

class BuildSubItem extends StatelessWidget {
  const BuildSubItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
