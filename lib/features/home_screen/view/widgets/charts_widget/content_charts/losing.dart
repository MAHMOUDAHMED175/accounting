import 'package:flutter/material.dart';
class LosingMoney extends StatelessWidget {
  const LosingMoney({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
      height: 200,
      width: 300,
    );
  }
}
